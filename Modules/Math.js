/* way to export */
const sub = (a, b) => a - b;
const add = (a, b) => a + b;
const div = (a, b) => a / b;
const mul = (a, b) => a * b;

module.exports = {
    sub, add, div, mul
};

/* 2nd way to export */
// exports.add = (a, b) => a + b;
// exports.sub = (a, b) => a - b;

// module.exports🤡     exports.fn🗿